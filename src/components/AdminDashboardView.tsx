import React, { useEffect, useState } from 'react';
import { AlertCircle, ArrowLeft, CheckCircle2, ClipboardList, FileText, LoaderCircle, Mail, MessageSquareText, RefreshCw, Settings, ShieldCheck, Sparkles, UsersRound } from 'lucide-react';
import TramIALogo from './TramIALogo';
import AppVersion from './AppVersion';
import AdminCatalogView from './AdminCatalogView';
import AdminUsersView from './AdminUsersView';
import AdminContactView from './AdminContactView';
import AdminOperationsView from './AdminOperationsView';
import AdminAdvisorsView from './AdminAdvisorsView';
import LoginView from './LoginView';
import AdminAuditView from './AdminAuditView';
import AdminLoginView from './AdminLoginView';
import AdminSettingsView from './AdminSettingsView';

type Payload = { summary:{users:{total:number;verified:number;active:number};procedures:{total:number;active:number;featured:number};messages:{total:number;pending:number};cases:{total:number;active:number;completed:number}};recentUsers:Array<{id:string;username:string;email:string;status:string;emailVerifiedAt?:string|null;createdAt:string}>;recentMessages:Array<{id:string;name:string;email:string;topic:string;status:string;createdAt:string}>;generatedAt:string };
const formatDate=(value:string)=>new Intl.DateTimeFormat('es-PE',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(value));

export default function AdminDashboardView({onExit}:{onExit:()=>void}){
 const [data,setData]=useState<Payload|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[authRequired,setAuthRequired]=useState(false);
 const [module,setModule]=useState<'dashboard'|'catalog'|'users'|'contact'|'operations'|'advisors'|'audit'|'settings'>(()=>new URLSearchParams(location.search).get('module')==='settings'?'settings':'dashboard');
 const load=async()=>{setLoading(true);setError('');setAuthRequired(false);try{const response=await fetch('/api/v1/admin/dashboard',{credentials:'include'});const result=await response.json().catch(()=>({}));if(response.status===401){setAuthRequired(true);return}if(!response.ok)throw new Error(result.message||'No pudimos cargar el panel.');setData(result)}catch(e){setError(e instanceof Error?e.message:'No pudimos cargar el panel.')}finally{setLoading(false)}};
 useEffect(()=>{void load()},[]);
 if(authRequired)return <AdminLoginView onSuccess={()=>void load()} onExit={onExit}/>;
 if(module==='settings')return <div className="min-h-screen bg-[#f5f3ff] text-slate-950"><header className="border-b border-violet-100 bg-white"><div className="mx-auto flex min-h-17 max-w-5xl items-center px-5"><TramIALogo iconSize={31} textSize="text-xl" variant="light" onClick={onExit}/><span className="ml-4 rounded-full bg-violet-100 px-3 py-1 text-[10px] font-black uppercase text-violet-700">Configuración</span><button onClick={()=>setModule('dashboard')} className="ml-auto rounded-xl border border-violet-200 px-4 py-2 text-xs font-black text-violet-700">Volver</button></div></header><main className="p-5 sm:p-8"><AdminSettingsView/></main></div>;
 if(authRequired)return <div className="min-h-screen bg-slate-100 text-slate-950"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex min-h-17 max-w-[1500px] items-center gap-4 px-4 sm:px-6"><TramIALogo iconSize={31} textSize="text-xl" variant="light" onClick={onExit}/><span className="rounded-full bg-blue-50 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.14em] text-blue-700">Administración</span><button onClick={onExit} className="ml-auto inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-black text-slate-600"><ArrowLeft size={15}/>Volver a TramIA</button></div></header><main className="mx-auto max-w-5xl p-4 py-8 sm:p-8"><div className="mb-6 text-center"><p className="text-xs font-black uppercase tracking-[.18em] text-blue-600">Acceso protegido</p><h1 className="mt-2 text-3xl font-black">Inicia sesión como administrador</h1><p className="mt-2 text-sm text-slate-500">Usa tu cuenta TramIA. Si tiene el rol correspondiente, entrarás directamente al panel.</p></div><LoginView initialMode="login" onAuthSuccess={()=>void load()}/></main></div>;
 if(module!=='dashboard')return <div className="min-h-screen bg-slate-100 text-slate-950"><header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex min-h-17 max-w-[1500px] items-center gap-4 px-4 sm:px-6 lg:px-8"><TramIALogo iconSize={31} textSize="text-xl" variant="light" onClick={onExit}/><span className="rounded-full bg-blue-50 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.14em] text-blue-700">{module==='catalog'?'Catálogo':module==='users'?'Usuarios':module==='contact'?'Contacto':module==='operations'?'Operación':module==='audit'?'Auditoría':'Asesores'}</span><button onClick={()=>setModule('dashboard')} className="ml-auto inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-black text-slate-600"><ArrowLeft size={15}/> Volver al resumen</button></div></header><main className="mx-auto max-w-[1500px] p-4 sm:p-6 lg:p-8">{module==='catalog'?<AdminCatalogView/>:module==='users'?<AdminUsersView/>:module==='contact'?<AdminContactView/>:module==='operations'?<AdminOperationsView/>:module==='audit'?<AdminAuditView/>:<AdminAdvisorsView/>}</main></div>;
 return <div className="min-h-screen bg-slate-100 text-slate-950"><header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex min-h-17 max-w-[1500px] items-center gap-4 px-4 sm:px-6 lg:px-8"><TramIALogo iconSize={31} textSize="text-xl" variant="light" onClick={onExit}/><span className="hidden h-7 w-px bg-slate-200 sm:block"/><span className="rounded-full bg-blue-50 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.14em] text-blue-700">Administración</span><button onClick={onExit} className="ml-auto inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-black text-slate-600 hover:bg-slate-50"><ArrowLeft size={15}/> Volver a TramIA</button></div></header><main className="mx-auto max-w-[1500px] p-4 sm:p-6 lg:p-8"><section className="relative overflow-hidden rounded-[2rem] bg-[linear-gradient(120deg,#061735,#0d4db7_60%,#13afd1)] p-6 text-white shadow-xl sm:p-8"><div className="absolute inset-0 opacity-20 bg-[radial-gradient(circle_at_1px_1px,white_1px,transparent_0)] bg-size-[22px_22px]"/><div className="relative grid items-center gap-5 md:grid-cols-[1fr_220px]"><div><p className="text-xs font-black uppercase tracking-[.18em] text-cyan-200">Centro de control</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">Panel administrativo</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-blue-100">Supervisa usuarios, contenido, consultas y operación desde una vista protegida.</p></div><img src="/assets/mascot/tramia-bot-reading.png" alt="TramIA revisa el panel administrativo" className="mx-auto hidden h-40 object-contain drop-shadow-xl md:block"/></div></section>
 {loading?<div className="mt-8 flex min-h-72 items-center justify-center rounded-3xl border border-blue-100 bg-white"><LoaderCircle className="animate-spin text-blue-600" size={34}/></div>:error?<div className="mt-8 rounded-3xl border border-red-200 bg-red-50 p-8 text-center"><AlertCircle className="mx-auto text-red-600" size={36}/><h2 className="mt-3 text-lg font-black">Acceso no disponible</h2><p className="mt-2 text-sm text-red-700">{error}</p><button onClick={load} className="mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl bg-red-600 px-4 text-sm font-black text-white"><RefreshCw size={16}/> Reintentar</button></div>:data&&<><section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={UsersRound} label="Usuarios" value={data.summary.users.total} detail={`${data.summary.users.verified} con correo verificado`} tone="blue"/><Metric icon={FileText} label="Trámites publicados" value={data.summary.procedures.active} detail={`${data.summary.procedures.total} en el catálogo`} tone="cyan"/><Metric icon={ClipboardList} label="Gestiones activas" value={data.summary.cases.active} detail={`${data.summary.cases.completed} completadas`} tone="violet"/><Metric icon={MessageSquareText} label="Consultas pendientes" value={data.summary.messages.pending} detail={`${data.summary.messages.total} recibidas`} tone="amber"/></section><section className="mt-6 grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><AdminCard title="Usuarios recientes" icon={UsersRound}><div className="divide-y divide-slate-100">{data.recentUsers.map(user=><div key={user.id} className="flex flex-col gap-2 py-3 sm:flex-row sm:items-center"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-blue-50 text-xs font-black text-blue-700">{user.username.slice(0,2).toUpperCase()}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-black">{user.username}</p><p className="truncate text-xs text-slate-500">{user.email}</p></div><div className="text-left sm:text-right"><span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[10px] font-black ${user.emailVerifiedAt?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-800'}`}>{user.emailVerifiedAt?<CheckCircle2 size={12}/>:<Mail size={12}/>} {user.emailVerifiedAt?'Verificado':'Pendiente'}</span><p className="mt-1 text-[10px] text-slate-400">{formatDate(user.createdAt)}</p></div></div>)}</div></AdminCard><div className="space-y-6"><AdminCard title="Módulos" icon={Settings}><div className="grid gap-3"><Module icon={FileText} title="Contenido y trámites" text="Categorías, entidades y catálogo" onClick={()=>setModule('catalog')}/><Module icon={UsersRound} title="Usuarios y roles" text="Permisos y estado de cuentas" onClick={()=>setModule('users')}/><Module icon={MessageSquareText} title="Bandeja de contacto" text="Consultas y seguimiento" onClick={()=>setModule('contact')}/><Module icon={ShieldCheck} title="Operación de trámites" text="Seguimiento, alertas y excepciones" onClick={()=>setModule('operations')}/><Module icon={UsersRound} title="Asesores y delegaciones" text="Perfiles, capacidad y asignaciones" onClick={()=>setModule('advisors')}/></div></AdminCard><div className="rounded-3xl bg-slate-950 p-5 text-white"><Sparkles className="text-cyan-300"/><h3 className="mt-3 font-black">Base administrativa lista</h3><p className="mt-2 text-xs leading-5 text-slate-300">El siguiente bloque habilitará el CRUD de categorías, entidades y trámites.</p><AppVersion className="mt-4 block text-[10px] text-slate-500"/></div></div></section></>}</main></div>;
}
function Metric({icon:Icon,label,value,detail,tone}:{icon:React.ElementType;label:string;value:number;detail:string;tone:'blue'|'cyan'|'violet'|'amber'}){const colors={blue:'bg-blue-50 text-blue-600',cyan:'bg-cyan-50 text-cyan-600',violet:'bg-violet-50 text-violet-600',amber:'bg-amber-50 text-amber-600'};return <article className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><span className={`grid size-12 place-items-center rounded-2xl ${colors[tone]}`}><Icon size={23}/></span><p className="mt-4 text-xs font-black uppercase tracking-widest text-slate-500">{label}</p><strong className="mt-1 block text-3xl font-black">{value}</strong><p className="mt-1 text-xs text-slate-500">{detail}</p></article>}
function AdminCard({title,icon:Icon,children}:{title:string;icon:React.ElementType;children:React.ReactNode}){return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={19}/></span><h2 className="text-lg font-black">{title}</h2></div><div className="mt-4">{children}</div></section>}
function Module({icon:Icon,title,text,onClick}:{icon:React.ElementType;title:string;text:string;onClick?:()=>void}){const C=onClick?'button':'div';return <><C onClick={onClick} className="flex w-full gap-3 rounded-2xl border border-slate-100 bg-slate-50 p-3 text-left hover:border-blue-200 hover:bg-blue-50"><Icon className="mt-0.5 shrink-0 text-blue-600" size={18}/><div><p className="text-xs font-black">{title}</p><p className="mt-1 text-[11px] leading-4 text-slate-500">{text}</p></div></C>{title==='Asesores y delegaciones'&&<button onClick={()=>location.assign('/admin?module=settings')} className="flex w-full gap-3 rounded-2xl border border-violet-200 bg-violet-50 p-3 text-left text-violet-800"><Settings className="mt-0.5 shrink-0" size={18}/><div><p className="text-xs font-black">Configuración general</p><p className="mt-1 text-[11px]">Canales de atención y datos públicos</p></div></button>}</>}






