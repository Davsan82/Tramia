import React, { useState } from 'react';
import { CheckCircle2, Clock3, Headphones, Mail, MapPin, MessageSquareText, Phone } from 'lucide-react';
import InstitutionalLayout, { ContentCard } from './InstitutionalLayout';

export default function ContactView({ onBack }: { onBack: () => void }) {
  const [form,setForm]=useState({name:'',email:'',phone:'',topic:'consulta',message:''}); const [state,setState]=useState<'idle'|'sending'|'sent'|'error'>('idle'); const [error,setError]=useState('');
  async function submit(e:React.FormEvent){e.preventDefault();setState('sending');setError('');try{const response=await fetch('/api/v1/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,sourcePath:window.location.pathname})});const raw=await response.text();let result:{message?:string;received?:boolean}={};if(raw){try{result=JSON.parse(raw);}catch{if(!response.ok)throw new Error('El servicio de contacto no está disponible. Reinicia el servidor e inténtalo nuevamente.');throw new Error('El servidor no devolvió una respuesta válida. Inténtalo nuevamente.');}}if(response.status===404)throw new Error('El servicio de contacto no está disponible. Reinicia el servidor e inténtalo nuevamente.');if(!response.ok)throw new Error(result.message||'No pudimos registrar tu mensaje.');if(!result.received)throw new Error('No pudimos confirmar la recepción de tu mensaje.');setState('sent');setForm({name:'',email:'',phone:'',topic:'consulta',message:''});}catch(e){setState('error');setError(e instanceof Error?e.message:'No pudimos registrar tu mensaje.');}}
  return <InstitutionalLayout eyebrow="Estamos para ayudarte" title="Contacto" intro="Cuéntanos qué necesitas. Te responderemos con información clara y sin pedirte contraseñas ni credenciales de entidades." mascot="/assets/mascot/tramia-bot-contact.png" mascotAlt="TramIA atiende tu consulta con audífonos" onBack={onBack} badge="Atención temporal: lunes a viernes, 9:00 a. m. a 6:00 p. m." showBackToTop={false}>
    <div className="grid gap-6 lg:grid-cols-[.72fr_1.28fr]">
      <div className="space-y-4"><ContentCard icon={Headphones} title="Canales de atención"><div className="space-y-4"><Info icon={Mail} title="Correo" text="davsan82@gmail.com"/><Info icon={Phone} title="Teléfono demo" text="+51 1 700 8080"/><Info icon={MapPin} title="Ubicación" text="Lima, Perú"/><Info icon={Clock3} title="Tiempo estimado" text="Dentro de 2 días hábiles"/></div></ContentCard><div className="rounded-2xl border border-cyan-200 bg-cyan-50 p-5 text-xs leading-5 text-slate-700"><strong>Cuida tus datos:</strong> nunca envíes contraseñas, Clave SOL, CVV, números completos de tarjeta ni documentos sensibles mediante este formulario.</div></div>
      <section className="rounded-3xl border border-blue-100 bg-white p-5 shadow-[0_18px_60px_rgba(27,72,140,.10)] sm:p-7"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-blue-50 text-blue-700"><MessageSquareText size={22}/></div><div><h2 className="text-xl font-black">Envíanos un mensaje</h2><p className="mt-1 text-xs text-slate-500">Todos los campos marcados son obligatorios.</p></div></div>{state==='sent'?<div className="mt-8 rounded-2xl border border-emerald-200 bg-emerald-50 p-7 text-center"><CheckCircle2 size={42} className="mx-auto text-emerald-600"/><h3 className="mt-3 text-lg font-black">Mensaje recibido</h3><p className="mt-2 text-sm text-slate-600">Gracias por escribirnos. Te responderemos al correo indicado.</p><button onClick={()=>setState('idle')} className="mt-5 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-extrabold text-white">Enviar otro mensaje</button></div>:<form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-2"><Field label="Nombre completo"><input className="field-input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required maxLength={120}/></Field><Field label="Correo electrónico"><input className="field-input" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required maxLength={320}/></Field><Field label="Celular (opcional)"><input className="field-input" type="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} maxLength={24}/></Field><Field label="Motivo"><select className="field-input" value={form.topic} onChange={e=>setForm({...form,topic:e.target.value})}><option value="consulta">Consulta general</option><option value="soporte">Soporte con mi cuenta</option><option value="tramite">Ayuda con un trámite</option><option value="privacidad">Privacidad y datos</option><option value="alianza">Alianzas</option></select></Field><label className="sm:col-span-2"><span className="mb-1.5 block text-xs font-extrabold text-slate-700">Mensaje</span><textarea className="field-input min-h-36 resize-y" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} required minLength={10} maxLength={2000} placeholder="Describe tu consulta con el detalle necesario, sin incluir contraseñas ni datos sensibles."/><span className="mt-1 block text-right text-[11px] text-slate-400">{form.message.length}/2000</span></label>{state==='error'&&<div role="alert" className="sm:col-span-2 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</div>}<label className="sm:col-span-2 flex items-start gap-2 text-xs leading-5 text-slate-600"><input type="checkbox" required className="mt-1 accent-blue-600"/>Acepto que TramIA utilice estos datos para responder mi solicitud, según su Política de Privacidad.</label><button disabled={state==='sending'} className="sm:col-span-2 min-h-12 rounded-xl bg-blue-600 px-5 text-sm font-extrabold text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700 disabled:opacity-60">{state==='sending'?'Enviando…':'Enviar mensaje'}</button></form>}</section>
    </div>
    <aside aria-label="Espacio informativo" className="relative isolate min-h-44 overflow-hidden rounded-3xl bg-[linear-gradient(105deg,#071a3d_0%,#0e48aa_55%,#13afd1_100%)] px-6 py-7 text-white shadow-[0_18px_55px_rgba(15,72,170,.18)] sm:px-9 lg:min-h-48">
      <div className="absolute inset-0 -z-10 opacity-20 bg-[radial-gradient(circle_at_1px_1px,white_1px,transparent_0)] bg-size-[22px_22px]" />
      <div className="absolute -right-14 -top-24 -z-10 size-72 rounded-full bg-cyan-300/25 blur-2xl" />
      <div className="max-w-2xl"><span className="inline-flex rounded-full border border-white/25 bg-white/10 px-3 py-1 text-[10px] font-black uppercase tracking-[.16em]">Espacio informativo</span><h2 className="mt-4 text-2xl font-black tracking-tight sm:text-3xl">Muy pronto, beneficios para hacer tus trámites más simples</h2><p className="mt-3 max-w-xl text-sm leading-6 text-blue-50">Este espacio está preparado para campañas, alianzas y anuncios útiles para la comunidad TramIA.</p></div>
      <div className="pointer-events-none absolute bottom-0 right-5 hidden items-end gap-3 sm:flex"><div className="mb-7 rounded-2xl border border-white/20 bg-white/10 px-4 py-3 text-xs font-bold backdrop-blur">Información clara, siempre a la mano.</div><img src="/assets/mascot/tramia-bot-contact.png" alt="" className="h-40 w-32 object-contain object-bottom drop-shadow-[0_16px_20px_rgba(0,0,0,.22)] lg:h-44 lg:w-40" /></div>
    </aside>
  </InstitutionalLayout>;
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label><span className="mb-1.5 block text-xs font-extrabold text-slate-700">{label}</span>{children}</label>}
function Info({icon:Icon,title,text}:{icon:React.ElementType;title:string;text:string}){return <div className="flex gap-3"><div className="grid size-9 shrink-0 place-items-center rounded-lg bg-blue-50 text-blue-700"><Icon size={17}/></div><div><p className="text-xs font-extrabold text-slate-950">{title}</p><p className="text-xs text-slate-500">{text}</p></div></div>}
