import React,{useEffect,useState}from'react';import{LoaderCircle,Save,Settings}from'lucide-react';const defaults={email:'',phone:'',location:'Lima, Perú',schedule:'Lunes a viernes, 9:00 a. m. a 6:00 p. m.',responseTime:'Dentro de 2 días hábiles'};
export default function AdminSettingsView(){const[form,setForm]=useState(defaults),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[message,setMessage]=useState('');useEffect(()=>{fetch('/api/v1/admin/settings',{credentials:'include'}).then(r=>r.json()).then(p=>{const row=(p.settings||[]).find((x:any)=>x.key==='contact');if(row?.value)setForm({...defaults,...row.value});setLoading(false)})},[]);const save=async()=>{setSaving(true);const r=await fetch('/api/v1/admin/settings/contact',{method:'PUT',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({value:form,isPublic:true})});setMessage(r.ok?'Configuración publicada correctamente.':'No pudimos guardar la configuración.');setSaving(false)};return <div className="mx-auto max-w-3xl"><p className="text-xs font-black uppercase tracking-widest text-violet-600">Configuración global</p><h2 className="mt-2 text-3xl font-black">Canales de atención</h2><p className="mt-2 text-sm text-slate-500">Estos datos se reflejan en todas las vistas públicas.</p>{loading?<LoaderCircle className="mx-auto mt-12 animate-spin text-violet-600"/>:<section className="mt-7 rounded-3xl bg-white p-6 shadow-sm"><span className="grid size-12 place-items-center rounded-2xl bg-violet-50 text-violet-600"><Settings/></span><div className="mt-5 grid gap-4 sm:grid-cols-2">{Object.entries({email:'Correo',phone:'Teléfono',location:'Ubicación',schedule:'Horario',responseTime:'Tiempo de respuesta'}).map(([key,label])=><label key={key}><span className="mb-1 block text-xs font-black">{label}</span><input className="field-input" value={(form as any)[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}</div>{message&&<p className="mt-4 rounded-xl bg-violet-50 p-3 text-xs font-bold">{message}</p>}<button onClick={()=>void save()} disabled={saving} className="mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl bg-violet-600 px-5 text-sm font-black text-white"><Save size={16}/>{saving?'Guardando…':'Guardar y publicar'}</button></section>}</div>}
